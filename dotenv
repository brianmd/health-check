export smtp_username=<username on smtp server>
export smtp_password=<password on smtp server>
export smtp_address=<smtp domain name>
export smtp_port=587
export alert_emails=<list of email addresses>
export frequency=<frequency to check in minutes>

